# Minimal requirements for Microsoft Native Architecture
# No heavy ML dependencies - uses Azure managed services instead

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Azure SDKs (all managed services)
azure-storage-blob>=12.19.0
azure-cognitiveservices-vision-computervision>=0.9.0
azure-identity>=1.15.0
azure-keyvault-secrets>=4.7.0

# Database (if using SQL)
# pyodbc>=4.0.39  # For Azure SQL Database
# pymssql>=2.2.11  # Alternative for Azure SQL

# Or use Azure Table Storage (simpler)
azure-data-tables>=12.4.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Minimal numpy if needed (much smaller than full ML stack)
numpy>=1.24.0

# Logging (optional, Azure has built-in logging)
loguru>=0.7.0


